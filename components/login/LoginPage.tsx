import React from "react";
import LoginCard from "../Cards/LoginCard";

const LoginPage = () => {
  return (
    <div className="max-w-[1280px] w-full h-screen flex items-center justify-center text-white bg-linear-to-r from-theme to-black">
      <LoginCard />
    </div>
  );
};

export default LoginPage;
